

# Makefile -- nie dziala
# src/libsdr.c -- kod ktory nas interesuje

# src/pylibsdr -- wrapper do Pythona
Pakiet "core" - dzialajace z HackRF
u≈ywac jedynie pakietu "mock" bez dostepu do HackRF - API powinno byc to samo co "core" (powinno byc chyba ze moje szare komoreczki cos przeoczyly); jedynie do testowania

